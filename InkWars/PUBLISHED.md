# Ink Wars 发布记录

## 0.1.3：大格城市街区预览修正

2026-09-10 已使用 Windows Publisher 1.1.0 发布并完成服务器回验。

- 版本：`0.1.3`；入口：`inkwars`；作者：`fwz233`
- 服务端清单：`C1PKG-PACKAGE 2`，`mode\tdirect`
- 签名目录序列：147
- 服务端包大小：1,356,797 字节
- 服务端包 SHA256：`92e5f022574b466080f3d17913a9ef3088dd47bc42e4b032f718939a0178f4fc`
- ELF SHA256：`e683bb96c9723fae72832a2553033dc0aa47261d6c7e59c1bd18476b415c828a`

本版修正预览图本身仍使用旧版 18×9、12px 稀疏构图的问题。现在预览与游戏实际布局一致，为全宽 12×5、24px 大格，并采用密集城市街区构图，重点展示建筑块、道路连接和树林体块在小屏幕上的实际可读比例。游戏程序同时包含上一版的大格地图布局和终端独占锁修复。

服务器公开目录的 Ed25519 签名验证通过；下载包的长度、SHA256、direct 清单和全部 11 个 payload 文件均与上传目录一致。验证记录：`build/publish-0.1.3-20260910-012012/server-verification.json`。

本次仍无 ADB 设备连接，未进行物理电子纸按键验收。更新时请刷新 APP 列表并安装完整的 `0.1.3` 软件包。

## 0.1.2：放大地图视觉比例


2026-09-10 已使用 Windows Publisher 1.1.0 发布并完成服务器回验。

- 版本：`0.1.2`；入口：`inkwars`；作者：`fwz233`
- 服务端清单：`C1PKG-PACKAGE 2`，`mode\tdirect`
- 签名目录序列：146
- 服务端包大小：1,356,690 字节
- 服务端包 SHA256：`25e239c77d7a64df735c301c24c75dc1785f87066e23610f02554fbd497c2c54`
- ELF SHA256：`e683bb96c9723fae72832a2553033dc0aa47261d6c7e59c1bd18476b415c828a`

本版将地图从 18×9 个 12px 小格改为全宽 12×5 个 24px 大格，重新布局地形、单位、迷雾、路径和光标，改善小屏幕可读性；同时保留 0.1.1 的 direct 启动与屏幕独占锁修复。

服务器公开目录的 Ed25519 签名验证通过；下载包的长度、SHA256、direct 清单和全部 11 个 payload 文件均与上传目录一致。验证记录：`build/publish-0.1.2-20260910-010348/server-verification.json`。

本次仍无 ADB 设备连接，未进行物理电子纸按键验收。更新时请安装完整包，确认 APP 版本为 `0.1.2`。

## 0.1.1：终端覆盖修复与地形细化


2026-09-09 已使用 Windows Publisher 1.1.0（`build/publisher/c1publish.exe`）上传正式软件仓库，并重新下载验证签名与包内容。

- ID：`inkwars`；显示名：`Ink Wars`；作者：`fwz233`
- 版本：`0.1.1`；入口：`inkwars`（MIPS 静态 ELF）
- 服务端包清单：`C1PKG-PACKAGE 2`，明确包含 `mode\tdirect`
- 下载验证时签名目录序列：141
- 服务端包大小：1,357,196 字节
- 服务端包 SHA256：`ee2ec2393e665b3e0d3a4901d1d5ce4465b0b0280c54d578bd1655ec6ced3394`
- 可执行文件 SHA256：`664666f9edc7477210e092b81b17a05d6466b8702cc4699e38cf7061cbaf1277`
- 对应源码 SHA256：`5572f0c30a027e9f99ecdeffc0a571df5489ce6aa74045c8431033b811170fdd`

沿用用户已批准的官方 HTTP 服务与作者令牌。令牌没有进入发布包。本地可信 Ed25519 公钥验证公开目录签名通过；按签名目录下载的压缩包长度、SHA256、direct 清单和全部 11 个 payload 文件均与冻结上传目录一致。验证使用官方固定 IP 回退。

发布目录：`build/publish-0.1.1-20260909-183956/`；其中 `server-verification.json` 为验证记录，`published.tar.gz` 为下载回验的正式包，`payload/` 保持上传时内容不变。

此版本同时修复 direct 启动模式、屏幕独占锁、终端回显和启动首帧调度，并细化 11 类地形及道路／河流／海岸邻接绘制。规则、UI、地形、多进程锁、设备运行分支模拟、MIPS 运行与独立源码重建测试通过；存档格式与 0.1.0 兼容。具体见 CHANGELOG.md。

发布后 ADB 仍无设备连接，因此没有自动安装，也没有完成本版物理电子纸按键复测。此前暂停启动器的真机测试漏测了终端竞争，后续必须保持启动器运行进行验收。

设备退出旧版后，在 APP 列表刷新并更新 Ink Wars；也可执行 `c1pkg update inkwars`，再执行 `c1pkg launch inkwars`。新安装可用 `c1pkg install inkwars`。应从软件仓库更新完整包，而不是仅替换 ELF，以确保 direct 模式清单也更新。

## 0.1.0：历史发布记录

2026-09-09 已使用 Windows `C1ancher-server/build/c1publish.exe` 上传正式软件仓库。

- ID：`inkwars`
- 显示名：`Ink Wars`
- 版本：`0.1.0`
- 作者：`fwz233`
- 入口：`inkwars`（MIPS 静态 ELF）
- 发布返回目录序列：138
- 服务端包大小：1,338,077 字节
- 服务端包 SHA256：`44c3f1dcfc235153b1f4952a7815f5e4041f545adfa0397dbf18c55dff89538b`
- 可执行文件 SHA256：`96cc236114619f3bcebde11c01075534efe09631ed48ff0568ee26138ae844c0`

经用户明确确认，使用已有作者令牌通过官方 HTTP 服务上传；令牌未写入发布包或本记录。HTTPS 目录接口当时返回 404。公开目录与发布包下载复核使用发布器源码所列的官方固定 IP 回退。

服务器入口校验要求 ELF，因此从 launch.sh 入口改为直接启动 inkwars；设备默认存档路径固定为 `/usr/data/inkwars/inkwars.sav`，不依赖工作目录。修改后主机规则 19,063 项、UI 闭环、MIPS ABI、独立源码重建一致性全部通过。此次没有重新执行真机测试或安装应用。

上传使用独立 payload，只包含程序、启动脚本、操作／测试／缺项文档、许可证，以及对应源码压缩包。上传后重新读取公共目录并下载服务端包，核对版本、作者、入口、长度、SHA256 和全部 10 个 payload 文件内容一致。验证记录位于 `build/publish-0.1.0-20260909-101348/server-verification.json`。

设备在 APP 列表刷新后可选择 Ink Wars 安装；也可执行 `c1pkg install inkwars`，之后 `c1pkg launch inkwars`。本次没有自动安装到设备。

此前 README / KNOWN_ISSUES / TESTING 中“尚未发布”的文字描述的是初始离线交付时点；本记录更新正式发布状态。`build/package` 本地工具输出仍是不含签名的离线包，服务器发布器重新封装的正式包具有以上独立哈希，两者不应混淆。保留已上传目录不变以便同版本重试，后续内容变化需升级版本。
