# 统一桌面设备安装记录：2026-09-19—24

## 最新结果：2.0.2 / sequence 26（2026-09-24，自动清理和界面修正）

本轮用户明确要求更新后默认清空 `/usr/data` 中除桌面及其运行配置以外的内容，并调整电池按键、取消选中行挖角、修正Shift管理箭头和本地应用更新提示。该要求取代旧版默认保留其他应用数据和备份的清理策略，详见 [清理范围与触发时机](update-cleanup.md)。

- 已刷入 `MagicPen-967fa9`，状态为 **`generation=142 phase=confirmed sequence=26 security_epoch=1 release=2.0.2`**。通过原 `prepare-local` / `tui-prepared` 流程约40秒自动确认，没有手改指针或强制确认；操作系统boot ID未变。关于页实际提交画面显示2.0.2。
- 当前核心为 `releases/26-2.0.2`，直接回滚为 `releases/25-2.0.1`。四组件全部从当前源码重新构建，不复用旧版桌面或启动器。实际桌面及启动器进程路径、二进制哈希均与本次签名包匹配。
- 自动清理已在真机执行并写入绑定清单的完成标记：`/usr/data` 根目录只剩 `c1`。已移除旧核心21–24、InkWars数据及测试目录、Pinao调试脚本与验证目录、历史备份、恢复下载、原厂OTA残留，以及 `book-reader`、`flomo`、`music-player`、`netease-music`、`pjournal` 在该分区的配置目录。不是只增加代码或手动执行临时删除命令。
- 清理后 `c1` 中剩余桌面配置/电池缓存/摘要、`bin`、`core`、`enabled`、`neofetch`、`pkg`、`update`、`wifi`。桌面和两份Wi-Fi配置、统计身份、原队列、启动文件和信任根哈希未变；`/storage` 的应用文件和当前链接、输入法0.1.1的39个文件全部保持原样。
- `/usr/data` 可用空间从 **41,106KiB 增至75,019KiB**，使用量从45,131KiB降至11,218KiB。该差值已包含新增26及保留25核心的占用。
- 功能修正：左右选择电池采样点，音量减/加切换日/时/分；Enter/OK不切换标签。桌面和应用选中行完整直角黑底，应用标签四角框保留；Shift箭头对称居中。本地列表显示“可更新”时，包括离线已验证缓存，确认进入管理提示；实际安装仍由原验证和明确选择完成，无更新直接启动。
- `make BUILD_DIR=build/revision26 -j4 -o neofetch-test host-test all lifecycle-test` 完整通过；Neofetch16项在普通WSL用户环境独立通过，其余使用隔离进程/挂载环境并恢复默认信号。清理覆盖15项，包括真实启动器在确认后后台执行、同版重启不重扫、下次更新再清理、未知/隐藏目录、长名称/深层目录及不越界。首次普通WSL测试受无关进程权限影响；随后修复优化构建发现的格式长度警告，并完整复跑通过。保留既有第三方编译、时钟偏差和jobserver警告。
- 签名包为 `build/desktop-bundle/26-2.0.2-device/core/`，四组件5,388,744字节；清单SHA-256为 **`aa7319f1cc95ec4fc9faf7071fef05d6e28e4a4260f74ab01c0176cdaee75505`**。序列26已占用，禁止签署其他内容。证据位于 `build/device-install-26-evidence/`。没有创建备份、部署服务器、公开发布、提交或推送。

- **实机界面验收已完成**：通过设备evdev注入导航按键，并读取已安装进程提交给屏幕的像素。关于页显示2.0.2；桌面和应用选中行四角均为完整黑底，Shift图标对称；单按Shift进入管理页。在本地列表选中网易云音乐（0.2.8，可更新到0.2.9）按确认，实际显示“有可用更新，是否更新？”及打开/更新/卸载/取消，而不是直接启动。电池日/时/分均由音量切换，Enter和OK都不切换；左右改变采样详情、不切换视图。未安装或启动第三方应用。图像见 `device-about-2.0.2.png`、`device-apps-*.png`、`device-app-update-prompt.png`、`device-battery-*.png`。这是程序实际提交画面，不是摄像头拍摄；实体键帽手感与墨水屏残影仍属人工验收。
- 验收中修正了临时诊断脚本的进程退出竞争，以及旧按键探针未接受音量/Shift/Enter的问题；没有因此改动或重签核心。结束时停留电池日视图，随后清除本次传输目录及四个临时诊断程序。

## 历史结果：2.0.1 / sequence 25（2026-09-24，补齐实际 UI 更新）

用户指出屏幕仍为旧版后，重新核对真机运行程序与本地源码，确认：序列24只更新启动器，桌面及应用管理仍为序列23确切字节；2026-09-23后续的四角选中标记、分钟电池历史和日/时/分视图此前确实未刷入。不能用序列21的早期三角标记/小时电池页记录代替后续需求的安装证据。

本轮已通过原签名更新流程安装到 `MagicPen-967fa9`，约35.4秒自动健康确认，最终 **`generation=137 phase=confirmed sequence=25 security_epoch=1 release=2.0.1`**。`current -> releases/25-2.0.1`，`previous -> releases/24-2.0.1`。

- **桌面 `C1ancher` 与应用管理 `c1pkg` 真正更新**，SHA-256分别为 `114f197962c59e1732a21594c23e15c31b6d5968b595976c7b8b15603e5fcea8`、`54d8d9ec36a8ee9cc7722da13d61617585e08a0f7eb68d119d79a288d329c2d6`。启动器和更新器复用已验签序列24字节，未夹带本地扩大分区清理范围的实现。外层更新器仍为保护槽b。
- 新核心为 `build/desktop-bundle/25-2.0.1-device/core/`，四组件合计5,314,788字节，清单SHA-256为 `dfd5e8c6480d9b8e5ce95fe6499b4309e21046fdc17cda3e886c587dc0048fec`。序列25已占用，禁止重签不同内容。版本显示仍为2.0.1；25是防降级序列。
- 实机通过evdev导航，读取**已安装进程已提交的像素缓冲**：电池日/时/分三视图按确认键循环、四角选中标记正确；应用本地/商店切换及替代原三角的四角括号正确，运行中的 `c1pkg` 哈希与新签名包一致。图像保存在 `build/device-install-25-evidence/device-battery-*.png`、`device-apps-*.png`。这不是主机模拟预览，也不是摄像头拍摄，实体键帽及墨水屏残影仍须人工确认。结束时停留在电池日视图。
- 补充电池v1→v2兼容迁移：保留原真实时间戳、电量和供电状态，旧小时点作为稀疏点显示，不补造分钟数据、不延续小时连线。升级时仍在24小时窗口内的12个旧观测逐项校验保留，新缓存已为 `C1BATTERY 2`。
- 核心 `host-test`、`lifecycle-test` 全部项目完成；Neofetch16项在正常WSL用户环境独立通过，其余套件在私有进程/挂载环境通过。首次主机运行有清理进程权限限制，隔离重试又暴露 `unshare --fork` 继承忽略SIGTERM/SIGINT，已在测试入口恢复默认信号后完整复跑通过；未放宽生产清理判断。GUI ASan/UBSan、电池ASan/UBSan及MIPS/QEMU电池回归通过，第三方Ed25519仍仅启用ASan。既有第三方编译和make jobserver提示保留，不宣称工具链零警告。
- 安装前后原输入法0.1.1的39文件、应用文件与链接、桌面及两份Wi-Fi配置、Neofetch、统计身份与原队列、两个信任根、启动文件均核验保持不变；界面导航后再次确认用户配置哈希不变。操作系统boot ID未变，无手改指针、强制确认或整机重启。
- **未执行分区清理**，原核心21–24及用户数据保留，当前新增25。只删除本次传输目录和三个临时诊断程序；没有创建备份、部署服务器、公开发布、Git提交或推送。安装后 `/usr/data` 可用约41,117KiB。

## 历史结果：2.0.1 / sequence 24（2026-09-24，启动器清理功能）

用户明确要求“刷到设备”。已通过既有签名更新流程将启动器更新安装到 USB 设备 `MagicPen-967fa9`，最终为 **`generation=132 phase=confirmed sequence=24 security_epoch=1 release=2.0.1`**；`current -> releases/24-2.0.1`、`previous -> releases/23-2.0.1`。约 **37.9 秒**自动健康确认，没有手改指针、强制确认或重启操作系统。

- **仅启动器二进制改变**。`C1ancher`、`c1pkg`、`c1updater` 复用已验签的序列23确切字节，不将工作区其他修改带入设备；外层更新器仍在保护槽 `b`。启动器、桌面实际运行路径均属于序列24。
- 本地新启动器静态 MIPS 构建、启动器及清理单元测试、完整 `make -j2 host-test lifecycle-test` 全部通过；本地及设备验签、目标ABI、六文件哈希与回滚序列23完整性复核通过。
- 签名包为 `build/desktop-bundle/24-2.0.1-device/core/`，四组件共 **5,310,684 字节**；清单 SHA-256 为 **`8849a73e77b692e9ea66c16a5298d08932ab328fc389737023068758da7d8bf6`**。序列24已占用，不得重签不同内容。仅为本地开发设备签署，未公开发布。
- **真机自动删除未完成，安全检查跳过清理**：设备 `/usr/data/c1` 为 `root:root / 0777`，属于所有用户可写的父目录，清理代码按既定安全策略拒绝从其下执行删除。用户随后明确选择保留该权限和已装序列24，接受暂时跳过自动清理；未放宽代码检查、未执行 `chmod`、未追加桌面重启或手动删除旧核心。目前保留21、22、23、24，23为直接回滚目标。不能把部署成功或主机测试通过表述为已在真机实现旧版本删除。
- 输入法 `0.1.1` 的39个文件、应用文件与链接、桌面与两份Wi-Fi配置、Neofetch、统计身份和原待报告队列、两个信任根及启动文件哈希均保持不变；操作系统boot ID不变。没有创建用户数据备份。
- 本次传输目录 `/storage/c1/desktop-install-core24-d13999cb4c0a` 已移除；本地只保留 `build/device-install-24-evidence/` 操作、构建和哈希证据。`/usr/data` 可用 **46,324 KiB**。没有部署服务器、提交、推送或刷写系统分区；实体按键、墨水屏与冷启动仍非本次验收范围。

## 历史结果：2.0.1 / sequence 23（2026-09-23，商店次数与关机监督修正）

用户明确要求“刷入”。已通过既有受信任更新流程将本地核心更新安装到 USB 设备 `MagicPen-967fa9`，最终为 **`generation=127 phase=confirmed sequence=23 security_epoch=1 release=2.0.1`**；`current -> releases/23-2.0.1`、`previous -> releases/22-2.0.1`。没有覆盖或重签序列22、手改指针、强制健康确认或主动重启操作系统。

- 新核心清单 SHA-256 为 **`adc44cbf45f604d5a7207356debee793da39a65d445dceb4bc29de405aa22ad2`**，四组件共 **5,306,544 字节**；四个组件和清单签名逐项验签、哈希及目标 MIPS ABI 核对通过。自动健康确认约 **41.5 秒**完成。
- 本轮使用已存在的核心信任根签署全新不可变序列23；活动更新器从保护槽 `a` 切换到 `b`，实际运行链为保护槽 `b` 更新器→序列23 launcher→序列23桌面。UI和launcher均核验到继承的关机通信通道，说明外层更新器已同步到本轮关机监督实现。
- 商店列表和详情显示 `N次`/“安装次数”，本地页面不显示统计标题、数值或占位符；底层同设备同应用重复安装/升级只计一次不变。
- 原输入法 `0.1.1` 的 **39 个文件**、应用文件与链接、用户配置、Wi-Fi 配置、Neofetch、统计身份与待上报队列、两个信任根和原厂启动文件均保持不变；序列22保留为直接回滚版本。本轮没有创建备份，也没有删除用户数据或旧核心。
- 本地完整回归及关机监督/运行时测试通过，传输目录 `/storage/c1/desktop-install-core23-19bb363f0774` 已清理。真实实体按键、拔电后的休眠/关机画面连续性仍需用户人工验收；自动健康确认不等同于这些硬件场景已验收。

## 历史结果：2.0.1 / sequence 22（2026-09-23，安装设备去重统计）

用户明确要求“刷入”，并在上线前确认先部署兼容旧接口的服务端，再更新 USB 真机。`MagicPen-967fa9` 已经原签名更新流程完成，最终 **`generation=122 phase=confirmed sequence=22 security_epoch=1 release=2.0.1`**；`current -> releases/22-2.0.1`、`previous -> releases/21-2.0.1`。没有覆盖序列21、手改指针、强制健康确认或重启操作系统。

- 刷入前完整 `make -j2 host-test all lifecycle-test` 通过，含新设备统计32项、旧统计23项；四个静态MIPS组件ABI核验、增量状态检查通过。四组件合计 **5,289,904 字节**；清单SHA-256 **`9c0a0c8e1c405cbb18f32e285356d536aa4af0993aad1c59a59807daf9466ec4`**。本地签名包为 `build/desktop-bundle/22-2.0.1-device/core/`，序列22已占用，禁止重签不同内容。
- 首次激活请求等待原应用管理界面释放占用，180秒观察超时期间一直为prepared，未回滚、未重复prepare。确认旧GUI的精确命令和可执行文件后正常发送SIGTERM，原有请求继续，约34.8秒自动健康确认。实际桌面路径、四组件和清单签名六文件哈希均匹配新版本。
- 原双信任根、输入法 `0.1.1` 的39文件、桌面及两份Wi-Fi配置、Neofetch五文件和操作系统boot ID均保持不变；两个旧域名待报告事件摘要不变，没有迁移或补造报告。
- 初始 `/usr/data` 仅5801KiB可用。用户确认清理历史后，只删除无运行进程或启动链接引用的核心序列10–19；序列22确认后再移除不再作为直接回滚的20。最终只保留当前22与直接回滚21，空闲 **56724KiB**。没有删除应用、媒体、词库、原厂文件或用户数据，没有创建备份；本次传输目录已清理。
- 配套服务端v2已上线，72项切换前后旧请求一致，外网24项固定旧请求一致；桌面每日内容按9月23日日期和既有语句规则单独验证，不能拿9月22日整段哈希作为跨日基线。旧统计 `hello=1` 保留；v2独立开始，没有伪造安装事件。
- 真机用未修改时钟和门限的生产v2同步接口，等原旧缓存门限自然到期后读到 **`available=1 count=14`**，14个应用均为明确0台。一时性检查程序仅在 `/tmp`，运行结束已删除。新GUI重开可读这个缓存；这证明设备读取/持久缓存成功，不等于已拍摄界面或完成生产重装去重验收。
- 用户此前已上报的hello事件属于旧口径，不自动补算v2。后续一次真实在线安装被新接口确认后才计入设备数；同设备同应用重装/升级只计一次。此次没有卸载/重装用户应用来制造验收数据，也未公开发布设备固件或提交推送源码。

## 历史结果：2.0.1 / sequence 21（2026-09-22，应用页、电池与 Neofetch）

用户明确要求“刷入”。已通过原签名更新流程将本地修改安装到 `MagicPen-967fa9`；最终 **`generation=117 phase=confirmed sequence=21 security_epoch=1 release=2.0.1`**，约 37.5 秒自动健康确认。`current -> releases/21-2.0.1`，`previous -> releases/20-2.0.1`。未手改指针、强制确认或重启操作系统。

- 核心包含浅底本地/商店标签的实心三角标识、顶部对应应用总数，以及用电池历史页面替换原进程页面。电池图显示最近 24 小时的每小时实际采样，可左右选点查看时分、电量和供电状态；采样中断显示断线，不能据此宣称关机或零耗电。设备已创建电池历史文件，长期曲线仍需积累真实样本。
- 刷入前完整 `host-test all lifecycle-test`、MIPS 四组件构建、相关电池/应用页/输入法测试与电池和 GUI ASan/UBSan 检查通过；增量构建确认没有待编译源文件。
- 核心包为 `build/desktop-bundle/21-2.0.1-device/core/`，四组件共 **5,281,684 字节**；清单 SHA-256 **`09cfef33a51950b4c3b123d74ef82bcafc772d29d8a71db9e805d159a5e9aeea`**。序列 **21 已占用，禁止重签不同内容**。签名、目标 ABI、设备固定恢复验证器及已安装六文件哈希均验证通过，实际运行桌面路径属于序列 21。
- Neofetch 不属于四组件核心，另行部署 `neofetch`、`c1-config.conf`、`c1-logo.txt` 三项；原 upstream 和许可文件核对一致、保留不动。小屏无参数调用自动省略大图标、限制行数和宽度并预留命令/提示符两行，显式参数与 `--stdout` 保留上游语义。
- 实机发现 BusyBox `stty size` 会让旧 `LINES/COLUMNS` 覆盖实际伪终端尺寸；只在尺寸探测子进程中清除两个变量，外层仍保留它们作为无有效尺寸时的后备值。补充 BusyBox 行为回归后 Neofetch **16/16** 主机测试通过。最终启动脚本 SHA-256 为 **`4def62a833b90393a65746fae82d3c66a141a3678f61d9ee2f191d865eeda31d`**，此独立资源修正没有修改已签核心。
- 设备原生 MIPS 测试程序使用真实伪终端，故意传入旧 `49×19` 环境变量，验证 `37×8` 输出 6 行、`37×6` 输出 4 行；标准输入为终端或 `/dev/null` 的四种情况全部通过，未输出网络名称或 IP。完整 `--stdout` 冒烟检查通过。这不等于人工实体按键和墨水屏残影验收。
- 两套信任根未变；输入法仍为原 `0.1.1`，与原序列 13 签名载荷的 **39 文件**全部一致。桌面设置与两份 Wi-Fi 配置安装前后哈希一致，操作系统 boot ID 未变，无待启动、回退或重启标记。
- 本地部署辅助程序最初误将 `prepare-local` 的输出按状态行解析；只读复核证明准备已成功且摘要一致，随后仅补记证据并继续原激活流程，没有重复准备或重新签名。
- 已删除本次设备传输目录 `/storage/c1/desktop-install-core21-2519b7bbfb7b` 和临时 PTY 程序。仅保留 `build/device-install-21-evidence/` 的本地操作与验证证据，没有创建用户数据备份、删除历史核心、部署服务器、提交或推送。清理后 `/usr/data` 可用 **5,798 KiB**，`/storage` 可用 **43,338,980 KiB**；下一次核心更新需要重新核对空间，不能自动删除回滚版本。

## 历史结果：2.0.1 / sequence 20（2026-09-21，Wi-Fi 初始化超时）

用户在序列 19 上反馈扫描失败、提示 `WLAN0 INITIALIZATION TIMED OUT`，本轮直接在同一 USB 设备诊断并修复。

- 现场只有 `lo`，没有 `wlan0`；目标驱动模块 `atbm603x_wifi_sdio` 存在且为 `live`，引用数为 0，没有依赖；厂商 `insmod_stat=0`。SDIO 设备 `007A:6011` 存在但未绑定驱动，无 supplicant/DHCP 进程。这确认了“驱动探测失败后模块残留，而旧代码仅等待接口”的失效状态。原始失败日志已不在内核缓冲区，最初为何探测失败尚不明确。
- 核对无接口、无绑定、无引用、无网络服务后，仅用普通 `rmmod atbm603x_wifi_sdio` 和 `insmod /etc/firmware/atbm603x_wifi_sdio.ko` 定向恢复。设备 uptime 从 21515.16 到 21516.09 秒，约 0.93 秒恢复 `wlan0`，`insmod_stat` 变为 1。没有运行含 `killall`/全局 rfkill 的厂商脚本。
- 代码增加一次受限重试：原等待最多 6 秒，只有明确失败且无人使用时才普通卸载并重载一次，额外最多 6 秒仍包含在操作总期限内；未知状态、模块依赖、绑定设备或网络服务存在则拒绝恢复，保留取消能力及凭据保护。中文界面增加驱动加载、初始化超时、恢复拒绝的中文提示。
- `build/wifi-recovery-20260921/` 完成全套 `host-test all lifecycle-test` 和静态 MIPS 四组件构建；针对性 Wi-Fi 服务测试、ASan/UBSan、真实控制套接字的隔离生命周期测试及中文提示像素/宽度检查通过。首次完整运行有既有更新检查超时用例的退出观察竞争（SIGKILL 已排队而 /proc 状态尚未转为 Z）；独立全部 27 项和完整复跑通过，未修改更新检查代码或测试。
- 核心包 `build/desktop-bundle/20-2.0.1-device/core/`，四组件总计 **5,257,048 字节**；清单 SHA-256 **`4a6a56d99e6ac86ddf35954f7ad53bfb95e6265415b4e2accce37632816b8cd0`**。序列 **20 已占用，禁止重签其他内容**。本轮只签核心，没有重建或重装输入法。
- 双信任根、原签名输入法 39 文件、目标 ABI、设备供电/空间、逐文件传输哈希及固定恢复验证器验证后，经原 `prepare-local` 和 `tui-prepared` 更新，约 39.5 秒自动健康确认。最终为 **`generation=112 phase=confirmed sequence=20 security_epoch=1 release=2.0.1`**；`current -> releases/20-2.0.1`、`previous -> releases/19-2.0.1`。未手改指针或强制确认。
- 生产 Wi-Fi 服务的 MIPS 临时验收程序在恢复后连续三次扫描成功，每次返回 8 项，没有输出 SSID 或密码。自动失败恢复的分支由主机故障夹具覆盖，没有故意在设备上再次制造探测失败。
- 自动确认后 USB 传输出现无响应。仅定向执行 ADB reconnect，随后发现操作系统 boot ID 已改变、uptime 约 124 秒；本轮没有发送整机重启命令，重启是否由用户操作尚未确认，没有 pstore 故障记录，不能宣称不存在设备异常。重连后再次验签/核对全部核心文件、实际运行桌面路径及序列 20 confirmed 状态，均通过；在模块尚未加载、无 wlan0 的此次启动状态下执行生产扫描程序，冷启动扫描成功返回 8 项。
- 输入法仍为原 `0.1.1`，39 文件全部一致。桌面设置 SHA-256 保持 `59dc3bf54f4ae4ac825b3b6f5b064f4bee7ebe9c6ef762daae1d1c8899bdaddd`；两份 Wi-Fi 配置哈希均与诊断前一致。没有清除或重写密码，没有保存用户配置/词库备份。
- 已删除设备暂存 `/storage/c1/desktop-install-core20-110784869132` 和 `/tmp/c1-wifi-scan-check-20260921`；仅保存工作区 `build/device-install-20-evidence/` 的状态/哈希/诊断证据。清理后 `/usr/data` 可用 **10,993 KiB**，`/storage` 可用 **43,338,972 KiB**。没有删除历史核心、用户应用或媒体，没有创建备份、提交、推送或部署服务器。

当前证明了实际无线接口恢复、重复/冷启动扫描和核心安装完整性；仍不等同于实体键帽交互、所有路由器认证、休眠恢复或整机重启原因已验收。

## 历史结果：2.0.1 / sequence 19（2026-09-21）

用户再次明确要求“刷入”。USB 恢复后确认唯一目标为 `MagicPen-967fa9`，已将当前本地代码通过原签名更新流程安装并自动健康确认。此次包含锁屏关机保持画面、Wi-Fi 冷启动/原厂控制接口兼容、锁屏文字确认与字符光标，以及 `PiN` 累计前缀定位修正；没有部署服务器或发布线上固件。

- 初始状态 `generation=102 phase=confirmed sequence=18 security_epoch=1 release=2.0.1`；设备 `mips`、root ADB、根分区只读、外部供电在线、电量 100%，没有待处理更新标记。两套设备信任根与本机公钥一致。
- 使用独立目录 `build/desktop-work-569feaf32be74fc9ad2e943012d9362b/core-build/` 完成 `host-test all lifecycle-test` 和四组件静态 MIPS 构建，输入法服务/SDK 的 MIPS/QEMU 验证及 14 项组合包测试通过。存在既有 libtsm 编译提示和 WSL 时钟偏差提示，不能宣称整个工具链零警告。
- 核心包 `build/desktop-bundle/19-2.0.1-device/core/`，四组件共 **5,252,948 字节**。清单 SHA-256：`4326059bdef84de7ed70fa6b631546e73d30593934e5b8d0a470c3992fdad443`。序列 **19 已占用，禁止重签不同内容**。
- 通用组合包安装首次被输入法完整性检查拦截：重建的 `luna_pinyin.reverse.bin` 和 `luna_pinyin.table.bin` 与原 `0.1.1` 不同。同版本安装实际跳过，没有覆盖原输入法，没有准备或切换核心。这不是设备原输入法损坏的证据。
- 随后独立验证序列 13 原签名输入法索引/包，逐一核对设备已装的 **39 个文件**，全部匹配；新旧输入法服务可执行文件哈希相同。改为只更新已签核心 19，保留原输入法服务、词典与用户数据，不安装组合包中新重建的同版本词典；没有签署序列 20。
- 再次核验本次暂存核心六个文件的 SHA-256、设备固定恢复验证器验签、供电与空间后，通过原 `c1updater prepare-local` 准备，状态为 `generation=105 phase=prepared sequence=19`。随后 `tui-prepared` 写入绑定清单摘要的原重启请求。未手改 current、未强制 confirm、未重启整个操作系统。
- 约 38 秒后自动健康确认：**`generation=107 phase=confirmed sequence=19 security_epoch=1 release=2.0.1`**。`current -> releases/19-2.0.1`，`previous -> releases/18-2.0.1`。运行桌面为 `/usr/data/c1/core/releases/19-2.0.1/artifacts/C1ancher`，输入法仍为 `/storage/c1/apps/c1-ime/versions/0.1.1/bin/c1-ime-service`，均只有一个进程。
- 四组件、清单及签名与本地签名包全部一致，设备固定恢复验证器复核成功。`desktop.conf` 前后 SHA-256 均为 `59dc3bf54f4ae4ac825b3b6f5b064f4bee7ebe9c6ef762daae1d1c8899bdaddd`，操作系统 boot ID 未变，没有待启动/回退/重启标记。
- 通用安装入口误建的本地备份 `build/device-install-19-temp/` 已删除；没有留下本次应用或用户词库备份。设备本次暂存目录 `/storage/c1/desktop-install-4881c08d2db148fb` 已删除。仅在 `build/device-install-19-evidence/` 保存安装脚本、状态及哈希元数据，不含配置内容或用户词库。
- 清理后 `/usr/data` 可用 **16,142 KiB**，`/storage` 可用 **43,338,972 KiB**。没有删除旧核心、回滚版本、用户应用或媒体，没有提交、推送、SSH 或服务器部署。

本次完成签名完整性、实际运行版本与自动健康确认。实体键帽操作、真实路由器连接、拔电休眠和关机保持画面仍须人工验收，不能将主机回归或正常启动视为这些场景全部通过。目录 `19-2.0.1-device/apps/` 中的重建输入法不是实际安装内容，整套组合包不能作为“已通过完整安装”分发；后续应提升输入法版本或复用原确切已签载荷。

## 历史结果：2.0.1 / sequence 18（2026-09-20）

用户在本地修改验收说明后明确要求“刷入”。本次已将浅色本地/商店标签、底部状态、三种电源模式和自适应文字锁屏安装到 USB 设备 `MagicPen-967fa9`，未部署服务器。

- 安装前为 `generation=97 phase=confirmed sequence=17 security_epoch=1 release=2.0.1`，设备外部供电在线、电量 100%、根分区只读，数据空间充足，无待处理更新标记。
- 刷入前完整 `make -j2 host-test all lifecycle-test` 通过。首次运行暴露测试脚本在进程编号文件刚创建但尚未写入时读取空内容；仅修正测试等待条件，相关 12 项进程测试重复通过，随后完整主机、引导恢复、运行日志、更新仓库及 HTTP 回归通过。此修正不改动生产 launcher。
- 使用原核心信任根签署新的不可变序列 `18`，版本号保持 `2.0.1`。包位于 `C1ancher/build/desktop-bundle/18-2.0.1-device/core/`；四组件共 5,248,864 字节，清单 SHA-256 为 `621e8acdbbe051bb4413ae73bf86c4291fa059ef377ad3a8c57fe27c7e69a99d`。禁止用序列 18 签署其他内容。
- 完成独立本地验签、目标 ABI 检查、逐文件传输哈希核验和设备固定恢复验证器验签，再由原更新器 `prepare-local`、`tui-prepared` 请求切换。未手工改 `current`，未强制确认，未重启整机系统。
- 自动健康确认后读回 `generation=102 phase=confirmed sequence=18 security_epoch=1 release=2.0.1`，`current -> releases/18-2.0.1`，`previous -> releases/17-2.0.1`。运行中的桌面为 `/usr/data/c1/core/releases/18-2.0.1/artifacts/C1ancher`；四组件、清单及签名共 6 个文件与本地签名包一致。
- 原输入法 `0.1.1` 的 39 个文件与序列 13 的原签名载荷一致并保留，未重装或改词典。`desktop.conf` 安装前后 SHA-256 均为 `31fd05e68fc1267f046185d72c28db05ec63f19e772c2a99f7677d0f4845cf2e`；旧电源配置由新核心在内存中采用标准模式，加载不改写文件。
- 没有创建备份，也未删除历史核心或用户数据。仅移除本次设备传输目录 `/storage/c1/desktop-install-core18-bf84dfb6b349`；清理后 `/usr/data` 可用 21,288 KiB，`/storage` 可用 43,338,972 KiB。直接回滚目标为序列 17。

本次完成的是签名完整性、实际运行版本和自动健康确认；没有进行实体按键、墨水屏视觉、拔电后的真实休眠或关机验收，不把主机测试或成功启动视作这些硬件场景已通过。没有公开发布、提交、推送或修改线上服务。

## 历史结果：2.0.1 / sequence 17（2026-09-20）

用户明确授权刷入 USB 设备。序列 17 已通过原核心信任根签名、设备固定恢复验证器验签和自动健康确认：`MagicPen-967fa9` 最终为 `generation=97 phase=confirmed sequence=17 security_epoch=1 release=2.0.1`，`current -> releases/17-2.0.1`，`previous -> releases/16-2.0.1`。清单 SHA-256 为 `fd2740bbd90f950c0a4933496ad41496d7ce9f044e40d67b25d27d2369a6129b`；四组件和清单共 6 个文件逐项核验，运行中的桌面路径属于序列 17。

本次不重刷分区、内核或根文件系统，不替换信任根，不手工切换 current，不强制确认，不部署服务器，不创建备份。设备输入法 `c1-ime 0.1.1` 的 39 个既有签名文件逐项一致，`desktop.conf` 安装前后 SHA-256 未变化；序列 16 保留为直接回滚版本。本次传输目录 `/storage/c1/desktop-install-core17-7f73f69c4658` 已删除，未删除应用、媒体、用户词库或核心回滚版本。

此次序列 17 安装核验的是签名、哈希、运行路径和自动健康确认，没有重新执行实体 Shift 按键、墨水屏显示或休眠/关机人工验收。浅色应用页标签、三种电源模式和自适应文字锁屏不属于序列 17，随后作为序列 18 安装，见文首。

## 历史结果：2.0.1 / sequence 16（2026-09-20）

此前按用户明确授权，将进程标题、GUI 字母定位、内部输入法服务隐藏及默认名言等改动安装到 `MagicPen-967fa9`。以下内容由保留的序列 16 安装、校验及清理元数据复核恢复，不代表本轮再次操作设备。

- 安装前为 `generation=87 phase=confirmed sequence=15 security_epoch=1 release=2.0.1`；使用既有核心信任根，未替换公钥。
- 核心序列 `16`、版本 `2.0.1`，四组件总计 5,244,764 字节；清单 SHA-256 为 `acf060632ba8d313ce8745049aac9b863d89313a000701d0080c475e37f095fa`。该序列不可再用于不同内容。
- 原更新流程完成自动健康确认后，读回 `generation=92 phase=confirmed sequence=16 security_epoch=1 release=2.0.1`，`current -> releases/16-2.0.1`，`previous -> releases/15-2.0.1`。四组件、清单及签名共 6 个文件匹配，运行中的桌面属于序列 16。
- 输入法 `0.1.1` 的 39 个文件与原序列 13 的签名载荷一致，配置文件安装前后未变，没有创建备份。
- 仅清除本次传输目录 `/storage/c1/desktop-install-core16-5b11f1f93a78`；清理后 `/usr/data` 可用 31,573 KiB、`/storage` 可用 43,340,984 KiB。安装校验记录不等于实体按键、联网摘要或休眠硬件验收。

## 历史结果：2.0.1 / sequence 15（2026-09-20）

用户再次明确授权“刷到这个 USB 连接的设备”。已完成 `MagicPen-967fa9` 的核心应用层更新，不重刷分区、内核或根文件系统：

- 安装前读回 `generation=82 phase=confirmed sequence=14 security_epoch=1 release=2.0.1`，两套信任根匹配、根分区只读、外部供电在线、电量 98%、数据分区空间足够，无待处理更新标记。
- 刷入前再次执行 `make BUILD_DIR=build/desktop-next -j4 host-test all lifecycle-test`，完整回归和静态 MIPS 构建通过。
- 使用现有核心密钥签署新的不可变序列 `15`；版本号保留 `2.0.1`。包位于 `C1ancher/build/desktop-bundle/15-2.0.1-device/core/`，清单 SHA-256 为 `88e41a83c8324c760321906ec7e8497b54a60bdd5221047710e335b16f924d7b`。禁止用序列 15 签署另一份内容。
- 原输入法 `0.1.1` 的 39 个文件与序列 13 的已签名输入法载荷逐一匹配。本次只更新核心中的前端/SDK 交互，未重建、覆盖输入法服务或词典。
- 逐文件传输校验、设备固定恢复验证器验签后，通过原更新器 `prepare-local` 和 `tui-prepared` 完成重启请求，未手工切换 current、未强制 confirm。
- 最终读回 `generation=87 phase=confirmed sequence=15 security_epoch=1 release=2.0.1`；`current -> releases/15-2.0.1`、`previous -> releases/14-2.0.1`，自动健康确认完成。四组件、清单和签名均与本地签名产物一致，配置文件 SHA-256 未变化，无回滚、待启动或重启标记。

### 本次设备验证与限制

- 两个 MIPS 原生生产前端测试程序连接设备上已运行的真实 Rime 服务，验证“你好”提交、音量翻页、左右高亮后确认、Shift+5/数字直选和中文应用搜索均通过。这是设备原生前端回归，不等于人工按实体键帽验收。
- 通过真实 evdev 注入，读取**当前已安装进程**的只读帧缓冲，验证主页四方向的选中项依次为 1、2、1、0；确认关于页实际显示 `2.0.1`，进程页显示终端、Wi-Fi、联网校时、后台检查四项且上下导航有效。没有切换这些开关。
- 原先依据 `refresh_cnt` 的验收方法在本机不可靠：`fast_refresh_only=1` 时该值停在 `refresh_max=30`。同时用户的三分钟锁屏可保留旧画面，方向键和 Home 在锁定时无动作，需要确认键解锁。此次改用软件绘制计数与实际高亮像素核验，不能把计数不变直接判为方向键失效。
- 内核未在所尝试的矩阵键盘事件节点声明 `KEY_TAB`，未把被丢弃的模拟事件算作表情实体键验收；该键及实际手感仍待人工确认。本轮没有声称应用列表的所有实机物理导航均重新完成。
- 设备 Wi-Fi 读回 `wpa_state=COMPLETED` 且具有 IPv4 地址；未更改保存的网络密码。此结果不覆盖所有路由器、断线重连或休眠恢复场景。
- 从设备请求配置仓库的 `/c1/v2/desktop.v1` 得到 HTTP **403**。在线每日名言和新增应用提示仍待服务端接口可用后做端到端验收；当前使用本地短句后备。本次未 SSH、部署或更改线上服务，也未发布设备核心版本到服务器。

### 清理与记录

遵照用户要求，**未创建任何备份**，未复制用户配置、媒体或词库。仅在工作区 `build/device-install-15/` 保存安装元数据、哈希、验收结果和屏幕诊断图像，不属于整机/应用数据备份。确认完成后已删除本次设备传输目录及四个 `/tmp` 诊断程序；未删除当前核心、序列 14 回滚核心、用户应用或媒体。清理后 `/usr/data` 可用 36,701 KiB，`/storage` 可用 43,340,984 KiB。未提交或推送。

## 历史结果：2.0.1 / sequence 14（2026-09-20）

用户要求修复方向键、升为 `2.0.1`、刷入 USB 设备，并删除低于 `2.0.0` 的旧核心。已对 `MagicPen-967fa9` 完成以下操作：

- 原运行状态读回为 `confirmed sequence=13 release=2.0.0`，不是仍停留在序列 12。
- 确定根因是主页模型将四个方向事件全部忽略。现改为上/左选上一项、下/右选下一项、首尾循环，确认才进入；表情键继续选择，不恢复方向键直接启动。
- 先在旧版本中通过真实 evdev 通道复现“主页方向键有事件却无刷新”；旧应用列表能响应注入事件，不能再把未经证明的 PTY 转发故障写成既定根因。
- 新增生产 `send_terminal_key -> libtsm -> c1_terminal_write -> 真实 PTY -> GUI` 回归，覆盖四方向、表情、CSI/SS3、延迟刷新期间的选中项变化；主机及静态 MIPS 测试程序在真实设备上均通过。
- 完整 `host-test all lifecycle-test`、运行词典 MIPS/QEMU 验证、签名校验及 GUI ASan/UBSan 回归通过（第三方 Ed25519 仍只启用 ASan，既有 libtsm 工具链告警保留）。
- 已安装 `2.0.1 / sequence 14`，原更新器请求重启后自动健康确认，最终状态 `generation=82 phase=confirmed sequence=14 security_epoch=1 release=2.0.1`。未手工改 current、未强制 confirm。
- 当前 `current -> releases/14-2.0.1`，`previous -> releases/13-2.0.0`；固定恢复验证器验签成功，四组件大小/哈希与签名包一致，无回滚、待启动或重启标记。
- 安装后通过设备 evdev 注入上/下/左/右：主页与应用列表每项均观察到屏幕刷新计数变化。自动化验收不是人工按实体键帽验收，最终手感仍由用户确认；不以刷新计数单独替代选中项语义回归。
- 输入法保持 `c1-ime 0.1.1`，逐文件与序列 13 已签名输入法载荷匹配。

### 签名身份和输入法打包限制

核心产物 `C1ancher/build/desktop-bundle/14-2.0.1-device/core/`，清单 SHA-256：`bfe712c1d75fbe9b6f1d8f1c2b9f4712e03cba92a0dc7fd53dcb2d0acb7fa587`。序列 14 已占用，不可再签不同核心内容。

完整桌面脚本重新生成 Rime 的 `luna_pinyin.reverse.bin` 和 `luna_pinyin.table.bin`，与旧 `0.1.1` 字节不同。安装器正确阻断同版本覆盖，因此该目录的组合包**不能作为已通过完整安装的输入法套件分发**。实际操作仅准备、激活核心 14，并以原序列 13 的签名索引核验设备现有输入法全部文件后保留它；没有绕过输入法完整性校验、没有重签不同内容的核心 14。后续重建词典应提升输入法版本或复用确切已签载荷。

### 工作目录备份和清理

序列 14 安装时曾在工作区创建 `D:\c1slim\build\device-backups\desktop-before-14-2.0.1-retry\`，以及第一次 USB 传输中断的 `desktop-before-14-2.0.1`。这两份备份已在用户要求后删除，不应再视为可用恢复材料；序列 15 安装没有重新创建它们。

新版本健康确认后，在原更新器的 POSIX 记录锁保护下，逐个核对电脑备份哈希及设备进程引用，再删除九个明确目录：`1-1.3.0`、`2-1.3.0`、`3-1.3.1`、`4-1.3.2`、`5-1.3.3`、`6-1.3.4`、`7-1.3.5`、`8-1.3.6`、`9-1.3.7`。释放 31,653 KiB（约 30.9 MiB），`/usr/data` 可用空间从安装后的 10,203 KiB 增至 **41,856 KiB（约 40.9 MiB）**。保留所有 `2.0.0` 核心，直接回滚目标为序列 13。两份本次安装暂存和 `/tmp` 诊断程序已删除。

未删除正式应用、媒体、用户词库、公钥、恢复验证器、更新状态或根分区内容；未部署服务器、公开发布、提交或推送。

### 后续边界测试

本轮复核另发现 GUI 的部分转义序列在消费者暂停超过 40ms 后可能丢弃已排队后缀，以及用户终端按住 OK 返回主页后控制键状态可能残留。它们没有被证明是本次主页失效根因，普通 CSI/SS3 与真机回归通过；后续需用故障注入和完整 evdev 状态转换单独验证修复。已签序列 14 保持不变，后续代码修复必须用更高序列。

## 以下为序列 12 的历史记录

## 授权与结果

用户在本地实现完成后明确要求“刷入”。本次已对 `MagicPen-967fa9` 执行签名应用层更新；未发布线上仓库、未提交或推送源码。

- 输入法 `c1-ime 0.1.0` 已通过现有设备应用公钥验签安装，安装后的所有载荷文件与签名包哈希逐项匹配。
- 核心版本 `2.0.0`、序列 **12** 已通过固定恢复验证器验证并由原更新器准备完成。
- 已通过 `c1updater tui-prepared` 发送现有重启请求，未手工改变 `current`，未强制确认。
- 请求后 USB/ADB 断开，持续观察超过 3 分钟未重新枚举。
- 用户随后明确确认：**已经显示新的中文桌面**。
- 因 ADB 尚未恢复，尚未读回自动健康确认状态，也未在设备上验证输入法协议 READY、实际中文提交或运行内存；不能把主机/QEMU 测试替代这些检查。

## 签名身份与存放位置

实际签名包：`C1ancher/build/desktop-bundle/12-device-20260919/`。

核心清单 SHA-256：`81af9c95a2c43dd843f53c4f95554e2670aac716c1a1f6e998e3e2199ad58416`。

输入法服务 SHA-256：`fa60737b232d9ac885e6b762cb85ec4950b73637fb1d30535155071c18ec900e`。

本机现有私钥仅由 OpenSSL 使用；从其派生的公钥指纹已与设备两套信任根一致。未生成新生产密钥、未替换设备公钥。

**序列 12 现已签过上述确切内容，禁止用序列 12 签署另一份不同核心清单。** 新内容使用更高序列；输入法载荷改变也应提高应用版本。

## 安装前基线

Linux 5.10.186，设备内存 51,524 KiB；外部供电在线，电池 98%。`/usr/data` 可用 12,712 KiB，`/storage` 可用 43,367,868 KiB。根分区只读。

当前可验签核心为 `releases/8-1.3.6`，前一代指针相同；更新状态为 `generation=67 phase=idle sequence=11 release=2.0.0`，是先前更新回退后保留的防降级状态。没有未完成回退、待启动或重启标记。线上 stable/canary 公共元数据经设备核心公钥验证均为序列 11。

未删除旧核心或其回滚材料。

## 备份与故障处理

完整应用层备份与安装证据：

- `D:\c1-backups\desktop-before-12-20260919\`
- `D:\c1-backups\desktop-before-12-20260919-retry2\`

范围为 `/etc/app_daemon`、`/usr/data/c1` 和若存在的输入法用户数据，不是整机、媒体或分区备份。备份可能含用户配置，只保留本地，不公开上传。

第一次传输因 Windows adb 将新文件赋为 0666 而被固定验签器拒绝，旧核心未改变。安装脚本现对自己新上传的文件收紧为 0600，再进行设备验签；没有放松验签器规则。第二次完成输入法安装和核心准备。

安装脚本同时支持验签通过且无待处理标记的 `idle`/`confirmed` 状态；仍要求新序列大于设备防降级序列。14 项主机回归全部通过，覆盖回退空闲状态、待处理状态阻断和上传权限收紧。

## 尚待连接恢复后的检查

1. 读取 `c1updater state`，核验 `phase=confirmed sequence=12` 与 `current -> releases/12-2.0.0`，使用固定恢复验证器复核当前核心。
2. 检查 `c1-ime-service` 的实际版本路径、单实例、socket READY 和真实中文提交；检查内存和内核 OOM 记录。
3. 验证 USB 重新连接及必要的冷启动；物理键盘、墨水屏、睡眠唤醒和续航仍需独立验收。

目前只证明“签名包已安装并准备、用户看到了新桌面”，不宣称上述后台验收已完成。待恢复 ADB 后继续只读检查，不反复刷同一版本、不强制 confirm。
