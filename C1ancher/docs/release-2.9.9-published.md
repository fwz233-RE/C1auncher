# 2.9.9 正式通道发布完成（2026-09-24）

## 线上状态

- 正式通道 `stable`：`27-2.9.9-fabe050dccb4c281`。
- 版本：`2.9.9`；签名序列：`27`；security epoch：`1`。
- 清单 SHA-256：`fabe050dccb4c2816400a37114fcf8ddc2c35ee57e280dae3e5527c86ec8a89d`。
- 发布源码本地提交：`6029ebcd9bade0ed53cda18adcba55452294007c`；由独立干净副本构建，无 `-AllowDirtySource`。
- 四组件总大小：5,388,744 字节。包仅包含桌面、应用管理器、启动器和更新器。
- 正式接口：`http://123.56.214.77/c1/core/v1/stable`。
- 测试通道仍为 `11-2.0.0-01d40bb99d9fc2cd`，链接 inode 和六文件摘要均未改变。本次直接发布正式通道，没有先发布测试通道。
- 输入法没有发布到商店，也没有打包进核心更新。未安装独立输入法的设备不因此获得中文输入服务。

## 已完成验证

- 当前四组件从源码重新编译，完整主机与生命周期回归通过；Neofetch 16 项测试和键盘布局验证另外通过。
- 本地与服务器规范校验器通过：Ed25519 签名、六文件集合、组件大小/SHA-256、MIPS32r2 小端 o32 硬浮点静态 ABI。
- 使用从线上下载并验签的序列11真实 MIPS 更新器，在 QEMU 中接受序列27的新签名清单；新桌面实际输出 `C1ancher 2.9.9`。该验证不是物理设备从11直升27的完整验收。
- 直接正式通道切换的隔离测试5项通过，覆盖测试通道不变、坏签名、禁用版本、旧正式指针发生变化和发布锁竞争。
- 公开接口13个文件的 GET、HEAD、大小和SHA-256全部通过；下载后的清单重新验签，四组件以独立 ELF 解析器复核架构。
- 最后服务器复核确认正式指针、六个文件和公开许可材料一致。c1repo/Caddy进程未重启，现有服务器激活/验签工具没有修改。

## 发布方法与限制

服务器已有发布器只提供先canary再promote的命令。本次在固定原工具摘要后，使用仅本次执行的适配，将原publish分支的目标改为stable，并增加锁内旧指针/摘要、全局已签序列及禁用序列检查；保留原验签、不可变目录、发布锁、原子rename和sync机制。原服务器工具文件保持不变。

`http://www.fwz233.com/c1/core/v1/stable/manifest.v1` 本轮仍为HTTP403，IP入口正常。不能据此保证仍只访问受阻域名且没有可用备用地址的旧设备已经能够联网更新。没有修改用户设备更新源、域名/DNS、代理配置或其他服务。

本次没有刷入任何设备。收尾ADB列表没有出现原C1设备，只出现无关设备，因此没有在其他设备执行更新检查或安装。

## 自动清理及公开材料

保留已实现的更新后默认清理 `/usr/data` 行为。版本说明明确：其他应用在该分区中的配置、阅读进度/书签、备份及调试残留会被删除；`/storage`原有应用、媒体和词库不删除；回滚核心不会恢复已删除数据。

- 版本说明：`http://123.56.214.77/downloads/c1ancher/2.9.9/release-notes.md`
- 对应源码：`http://123.56.214.77/downloads/c1ancher/2.9.9/C1ancher-2.9.9-corresponding-source.tar.gz`
- 源码包SHA-256：`3fc0697e72629f5f9e993ab97be662fa2f6a160175a355fbe04539aa170d5737`
- 校验清单：`http://123.56.214.77/downloads/c1ancher/2.9.9/SHA256SUMS`

仅分发允许公开的设备端对应源码、C输入客户端与第三方许可；未上传服务器、发布器、Windows安装器或InkWars源码。没有推送GitHub、创建GitHub Release或上传输入法包。原开发工作区未提交，既存索引在源码冻结过程中校验保持不变。

## 证据与后续约束

证据已归拢到主项目内 `build/release-2.9.9/`（维护者本机为 `D:\c1slim\build\release-2.9.9\`）：`validation.json`、`signed.json`、`old-updater-compatibility.json`、`stable-adapter-tests.json`、`server-staged.json`、`server-activate.json`、`server-verify.json`、`public-verified.json`。这些本地材料不提交到 GitHub。

原正式发布提交 `6029ebcd9bade0ed53cda18adcba55452294007c` 已导入主项目历史。独立发布副本只用于当时构建，后续开发统一在主项目进行；原签名核心及历史验证记录保持原字节，不重建或重签序列 27。本文前述“没有推送 GitHub”等描述记录的是服务器发布当时的状态，后续源码同步不等于创建新的 GitHub Release 或输入法上架。

序列27已用于正式签名及公开发布，绝不可重签不同内容。服务器保留原正式序列11和其他历史核心；本次未清理服务器历史版本。

早期独立测试遇到旧本地夹具已清除二进制、Windows文本换行和WSL文件权限问题，均在发布前修正夹具/材料后通过。首次公开下载之后的重复WSL校验因本地WSL启动异常未运行；随后以已有生产ELF验证实现完成独立复核，且服务端规范验签再次通过。没有因此重建或改写已签名核心。
